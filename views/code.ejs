<!DOCTYPE html>
<html lang="en">
<head>
    <% include partials/head.ejs %>
</head>
<body>
  <% include partials/nav.ejs %>
        

                    <h1 class="text-center">Code Snippits</h1>
                    <a class="nav-link" href="/code/newCode">Add Code</a>
               
                <% for (let i=0; i< code.length; i++) { %>
            <div class="card" style="width: 18rem;">
                <div class="card-body">
                  
                  <h5 class="card-title"><%= code[i].code; %></h5>
                  <h6 class="card-subtitle mb-2 text-muted"><% code[i].id; %></h6>
                  <p class="card-text"><%= code[i].description%></p>
                  <a href="/code/<%=code[i].id; %>" class="card-link"><%=code[i].code%></a>
                  <!-- <a href="#" class="card-link">Another link</a> -->
                </div>
                <form action="/code/<%=code[i].id; %>?_method=DELETE" method="POST">
                  <input type="submit" value="DELETE" />
      
              </form>
              </div>
              <br/>
              <%  } %>
</body>
</html>

